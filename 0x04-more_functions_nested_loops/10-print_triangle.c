#include "main.h"

/**
 * print_tringle - prints
 * @size: size of the triangle
 *
 * Return:always 0 ( success)
 */
void print _tringle(int size)
{
	if (size <= 0)
	{
		_putchar('\n');
	}
	else
	{
		int i, j;

		for (i = 1; i <= sie; i++)
		{
			_putchar(' ');
		}

		for (j = 1; j <= i; j++)
		{
			_putchar('#');
		}
		_putchaar('\n');
		}
	}
}
